---
name: Lint
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  build_push:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Lint
        run: |
          ./lint.sh
